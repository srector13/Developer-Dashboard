{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Everything the notebook UI needs. The dialog, opener, notification and global-shortcut plugins are driven from Rust, so no JS-side permissions are granted for them.",
  "windows": ["main", "capture", "launcher", "scratchpad", "region"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
