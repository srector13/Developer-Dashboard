{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Everything the dashboard and launcher need. The dialog, opener, notification and global-shortcut plugins are driven from Rust, so no JS-side permissions are granted for them — in particular the renderer cannot name a program to run.",
  "windows": ["main", "launcher"],
  "permissions": ["core:default"]
}
